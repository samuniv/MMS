services:
  app:
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - ASPNETCORE_URLS=http://+:5000
    volumes:
      - ./logs:/app/logs
      - ./uploads:/app/uploads
    build:
      context: .
      dockerfile: Dockerfile
      target: final